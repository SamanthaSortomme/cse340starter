<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> | CSE 340 App</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>

<h1><%= title %></h1>

<% if (messages() && messages().success) { %>
  <div class="flash-message success">
    <%= messages().success %>
  </div>
<% } else if (messages() && messages().error) { %>
  <div class="flash-message error">
    <%= messages().error %>
  </div>
<% } %>

<form action="/account/register" method="POST">
    <div>
      <label for="firstName">First Name:</label>
      <input type="text" id="firstName" name="firstName" required>
    </div>
    <div>
      <label for="lastName">Last Name:</label>
      <input type="text" id="lastName" name="lastName" required>
    </div>
    <div>
      <label for="email">Your Email:</label>
      <input type="email" id="email" name="email" required>
    </div>
    <div>
      <label for="password">Password:</label>
      <input type="password" id="password" name="password" pattern="^(?=.*[A-Z])(?=.*\d)(?=.*[@$!%*?&])[A-Za-z\d@$!%*?&]{12,}$" title="Password must be 12 characters or more, and include at least one uppercase letter, one number, and one special character" required>
    </div>
    <button type="submit">Register</button>
  </form>